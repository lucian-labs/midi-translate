{
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "lib": ["es2022", "dom"],
    "types": [],
    // Mirrors the alias in vite.demo.config.ts so the editor resolves the
    // library's types from the working tree rather than from node_modules.
    "paths": {
      "midi-translate": ["../index.d.ts"]
    }
  },
  "include": ["main.ts"]
}
